const API = "/api/v1";
const state = { executionId: null, timer: null, catalog: null, events: [], afterSequence: 0, controller: null, creating: false };
const esc = (value) => String(value ?? "").replace(/[&<>\"']/g, (c) => ({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
const api = async (path, options = {}) => { const response = await fetch(API + path, { credentials: "same-origin", headers: { "Content-Type": "application/json", ...(options.headers || {}) }, ...options }); const text = await response.text(); let data = null; try { data = text ? JSON.parse(text) : null; } catch { data = { detail: text }; } if (!response.ok) throw new Error(data?.detail || `HTTP ${response.status}`); return data; };
function ensureStyles(){ if(document.getElementById("instagram-dashboard-styles"))return; const style=document.createElement("style"); style.id="instagram-dashboard-styles"; style.textContent=`#view-instagram .ig-grid{display:grid;grid-template-columns:minmax(320px,1fr) minmax(420px,1.4fr);gap:16px}#view-instagram .ig-card{padding:18px;margin-bottom:16px}#view-instagram .ig-row{display:flex;gap:10px;align-items:center;flex-wrap:wrap}#view-instagram .ig-field{display:flex;flex-direction:column;gap:6px;margin:10px 0}#view-instagram .ig-field input,#view-instagram .ig-field select,#view-instagram .ig-field textarea{width:100%;box-sizing:border-box}#view-instagram .ig-checks{max-height:180px;overflow:auto;border:1px solid var(--border,#ddd);padding:8px;border-radius:8px}#view-instagram .ig-check{display:flex;gap:8px;padding:5px 2px}#view-instagram .ig-status{font-weight:700;text-transform:uppercase}#view-instagram .ig-muted{opacity:.7}#view-instagram .ig-table{width:100%;border-collapse:collapse}#view-instagram .ig-table th,#view-instagram .ig-table td{padding:7px;border-bottom:1px solid var(--border,#ddd);text-align:left;vertical-align:top}#view-instagram .ig-events{max-height:220px;overflow:auto;font-family:monospace;font-size:12px;white-space:pre-wrap}#view-instagram .ig-result{max-height:340px;overflow:auto;font-family:monospace;font-size:12px;white-space:pre-wrap}@media(max-width:900px){#view-instagram .ig-grid{grid-template-columns:1fr}}`; document.head.appendChild(style); }
function mount(){ ensureStyles(); const tabs=document.querySelector(".workspace-tabs"),views=document.querySelector("main.shell"); if(!tabs||!views||document.getElementById("view-instagram"))return; const tab=document.createElement("button"); tab.className="workspace-tab"; tab.dataset.view="instagram"; tab.textContent="Instagram"; tabs.appendChild(tab); const section=document.createElement("div"); section.id="view-instagram"; section.className="workspace-view"; section.innerHTML=`<div class="ig-grid"><section class="card ig-card"><div class="panel-head"><h2>Instagram · Nueva ejecución</h2></div><div class="ig-field"><label for="ig-capability">Operación</label><select id="ig-capability"><option value="instagram.prospecting">Prospección</option><option value="instagram.maduracion">Maduración</option></select></div><div class="ig-field"><span id="ig-task-types-label">Tipos de tarea</span><div id="ig-task-types" role="group" aria-labelledby="ig-task-types-label" class="ig-checks"><span class="ig-muted">Cargando catálogo…</span></div></div><div class="ig-field"><label for="ig-target-mode">Destino</label><select id="ig-target-mode"><option value="accounts">Cuentas seleccionadas</option><option value="owner">Propietario</option><option value="all">Todas las cuentas Instagram</option></select></div><div class="ig-field" id="ig-accounts-field"><span id="ig-accounts-label">Cuentas Instagram</span><div id="ig-accounts" role="group" aria-labelledby="ig-accounts-label" class="ig-checks"><span class="ig-muted">Cargando…</span></div></div><div class="ig-field" id="ig-owner-field" hidden><label for="ig-owner">Propietario</label><select id="ig-owner"></select></div><div class="ig-field"><label for="ig-schedule">Inicio programado (opcional)</label><input id="ig-schedule" type="datetime-local"></div><div class="ig-field"><label for="ig-custom">Tarea personalizada (JSON opcional)</label><textarea id="ig-custom" rows="3" placeholder='{"campaign_id":10}'></textarea></div><div class="ig-field"><label for="ig-executor">Bot executor (opcional)</label><input id="ig-executor" placeholder="Se resuelve en el backend si se deja vacío"></div><div class="ig-row"><button id="ig-run" class="button primary">Crear ejecución</button><span id="ig-create-message" class="ig-muted" aria-live="polite"></span></div></section><section class="card ig-card"><div class="panel-head"><h2>Seguimiento</h2><button id="ig-cancel" class="button danger" disabled>Cancelar</button></div><div id="ig-current" class="ig-muted">No hay ejecución seleccionada.</div><div id="ig-events" class="ig-events"></div><div class="panel-head"><h2>Resultado</h2></div><div id="ig-task-table" class="table-wrap"></div><pre id="ig-result" class="ig-result">Sin resultado.</pre></section></div><section class="card ig-card"><div class="panel-head"><h2>Historial Instagram</h2><button id="ig-refresh-history" class="button secondary">Recargar</button></div><div class="table-wrap"><table class="ig-table"><thead><tr><th>Execution</th><th>Capability</th><th>Estado</th><th>Creada</th><th>Acciones</th></tr></thead><tbody id="ig-history"></tbody></table></div></section>`; views.appendChild(section); tab.addEventListener("click",activate); document.getElementById("ig-target-mode").addEventListener("change",updateTargetFields); document.getElementById("ig-run").addEventListener("click",createExecution); document.getElementById("ig-cancel").addEventListener("click",cancelExecution); document.getElementById("ig-refresh-history").addEventListener("click",loadHistory); loadCatalog(); const existing=new URLSearchParams(location.search).get("execution_id");if(existing&&/^[0-9a-f-]{36}$/i.test(existing))startPolling(existing); }
function activate(){ document.querySelectorAll(".workspace-tab").forEach((button)=>button.classList.toggle("active",button.dataset.view==="instagram")); document.querySelectorAll(".workspace-view").forEach((view)=>view.classList.toggle("active",view.id==="view-instagram")); loadHistory(); }
async function loadCatalog(){ try{ state.catalog=await api("/instagram/catalog"); const taskBox=document.getElementById("ig-task-types"); taskBox.innerHTML=(state.catalog.task_types||[]).map((task)=>`<label class="ig-check"><input type="checkbox" value="${esc(task.id)}"><span>${esc(task.id)} · ${esc(task.task_name||task.descripcion||"Tarea")}</span></label>`).join("")||`<span class="ig-muted">No hay tareas Instagram disponibles.</span>`; const accountBox=document.getElementById("ig-accounts"); accountBox.innerHTML=(state.catalog.accounts||[]).map((account)=>`<label class="ig-check"><input type="checkbox" value="${esc(account.id)}"><span>${esc(account.id)} · ${esc(account.account_name||"Cuenta")}</span></label>`).join("")||`<span class="ig-muted">No hay cuentas Instagram disponibles.</span>`; document.getElementById("ig-owner").innerHTML=`<option value="">Seleccionar…</option>`+(state.catalog.owners||[]).map((item)=>`<option value="${esc(item.id)}">${esc(item.owner_name||item.id)}</option>`).join(""); }catch(error){ document.getElementById("ig-create-message").textContent=`Catálogo: ${error.message}`; } }
function updateTargetFields(){ const mode=document.getElementById("ig-target-mode").value; document.getElementById("ig-accounts-field").hidden=mode!=="accounts"; document.getElementById("ig-owner-field").hidden=mode!=="owner"; }
function selectedValues(selector){ return [...new Set([...document.querySelectorAll(selector+" input:checked")].map((el)=>Number(el.value)).filter((id)=>Number.isSafeInteger(id)&&id>0))]; }
async function createExecution(){ const message=document.getElementById("ig-create-message"),mode=document.getElementById("ig-target-mode").value,taskTypes=selectedValues("#ig-task-types"); if(!taskTypes.length){message.textContent="Selecciona al menos un tipo de tarea.";return;} const targets={mode}; if(mode==="accounts"){targets.account_ids=selectedValues("#ig-accounts");if(!targets.account_ids.length){message.textContent="Selecciona al menos una cuenta.";return;}} if(mode==="owner"){const owner=document.getElementById("ig-owner").value;if(!owner||!Number.isSafeInteger(Number(owner))||Number(owner)<=0){message.textContent="Selecciona un propietario válido.";return;}targets.owner_id=Number(owner);} let customTask={}; const custom=document.getElementById("ig-custom").value.trim(); if(custom){try{customTask=JSON.parse(custom);if(!customTask||Array.isArray(customTask)||typeof customTask!=="object")throw new Error("object required");}catch{message.textContent="El JSON de tarea personalizada no es válido.";return;}} const options={}; const executor=document.getElementById("ig-executor").value.trim();if(executor)options.bot_executor=executor;const capability=document.getElementById("ig-capability").value;const payload={schema_version:`${capability}.input.v1`,stage:capability==="instagram.maduracion"?"instagram_maduracion":"instagram_prospecting",capability,task_types:taskTypes,targets};if(customTask && Object.keys(customTask).length)payload.custom_task=customTask;if(Object.keys(options).length)payload.options=options;const scheduled=document.getElementById("ig-schedule").value;if(scheduled){const start=new Date(scheduled);if(Number.isNaN(start.getTime())){message.textContent="La fecha programada no es válida.";return;}payload.schedule={start_date:start.toISOString()};}if(mode==="all"&&!window.confirm("Se crearán tareas para todas las cuentas Instagram autorizadas. ¿Continuar?"))return;if(state.creating)return;state.creating=true;document.getElementById("ig-run").disabled=true;try{message.textContent="Creando…";const execution=await api("/executions/standalone/instagram",{method:"POST",body:JSON.stringify(payload)});const id=execution.id||execution.execution_id;if(!id)throw new Error("La API no devolvió el id de ejecución");state.executionId=id;document.getElementById("ig-cancel").disabled=!(["queued","running"].includes(execution.status));message.textContent=`Creada: ${id}`;document.getElementById("ig-current").textContent=`Execution: ${id}`;document.getElementById("ig-events").textContent="";document.getElementById("ig-result").textContent="Esperando resultado…";startPolling(id);await loadHistory();}catch(error){message.textContent=`Error: ${error.message}`;}finally{state.creating=false;document.getElementById("ig-run").disabled=false;} }
function startPolling(id){clearTimeout(state.timer);state.controller?.abort();state.executionId=id;state.events=[];state.afterSequence=0;const controller=new AbortController();state.controller=controller;const url=new URL(location.href);url.searchParams.set("execution_id",id);history.replaceState(null,"",url);const poll=async()=>{if(controller.signal.aborted)return;try{const execution=await api(`/executions/${encodeURIComponent(id)}`,{signal:controller.signal});if(controller.signal.aborted)return;renderExecution(execution);document.getElementById("ig-cancel").disabled=!(["queued","running"].includes(execution.status));await loadEvents(id,controller.signal);if(controller.signal.aborted)return;if(["succeeded","failed","cancelled"].includes(execution.status)){await loadResult(id);document.getElementById("ig-cancel").disabled=true;return;}}catch(error){if(controller.signal.aborted)return;document.getElementById("ig-current").textContent=`Error: ${error.message}`;state.timer=setTimeout(poll,10000);return;}state.timer=setTimeout(poll,4000);};poll();}
function renderExecution(execution){const id=execution.id||execution.execution_id||state.executionId;document.getElementById("ig-current").innerHTML=`<div class="ig-status">${esc(execution.status)}</div><div>${esc(id)}</div><div class="ig-muted">${esc(execution.requested_capability||execution.capability||"Instagram")}</div>`;}
async function loadEvents(id,signal){try{const events=await api(`/executions/${encodeURIComponent(id)}/events?after_sequence=${state.afterSequence}&limit=100`,{signal});if(signal.aborted)return;const fresh=Array.isArray(events)?events:events.items||[];state.events.push(...fresh);for(const event of fresh)state.afterSequence=Math.max(state.afterSequence,Number(event.sequence)||0);document.getElementById("ig-events").textContent=state.events.map((event)=>`[${event.sequence??"-"}] ${event.event_type||"event"}: ${event.summary||""}`).join("\n");}catch(error){if(error.name==="AbortError")return;}}
async function loadResult(id){try{const result=await api(`/executions/${encodeURIComponent(id)}/result`);if(state.executionId!==id)return;const payload=result.payload||result;const totals=payload.totals||{};const summary=`Creadas: ${totals.created??"-"} | Correctas: ${totals.ok??"-"} | Errores: ${totals.error??"-"} | Canceladas: ${totals.cancelled??"-"}`;document.getElementById("ig-result").textContent=summary+"\n\n"+JSON.stringify(payload,null,2);const container=document.getElementById("ig-task-table");container.replaceChildren();if(Array.isArray(payload.tasks)&&payload.tasks.length){const table=document.createElement("table");table.className="ig-table";const head=document.createElement("tr");for(const label of ["TaskBot","Cuenta","Estado","Ejecutor","Finalizada","Detalle"]){const cell=document.createElement("th");cell.textContent=label;head.appendChild(cell);}table.appendChild(head);for(const task of payload.tasks.slice(0,100)){const row=document.createElement("tr");for(const key of ["task_bot_id","account_id","status","bot_executor","end_date","comment"]){const cell=document.createElement("td");const value=task[key];cell.textContent=typeof value==="object"?JSON.stringify(value):String(value??"");row.appendChild(cell);}table.appendChild(row);}container.appendChild(table);if(payload.tasks.length>100){const note=document.createElement("p");note.textContent=`Mostrando 100 de ${payload.tasks.length} tareas. El JSON conserva el resultado completo.`;container.appendChild(note);}}}catch(error){if(state.executionId===id)document.getElementById("ig-result").textContent=error.message;}}
async function cancelExecution(){if(!state.executionId)return;const cancelId=state.executionId;document.getElementById("ig-cancel").disabled=true;if(!window.confirm("¿Cancelar la ejecución de Instagram?")){document.getElementById("ig-cancel").disabled=false;return;}try{await api(`/executions/${encodeURIComponent(cancelId)}/cancel`,{method:"POST",body:"{}"});if(state.executionId===cancelId)startPolling(cancelId);}catch(error){document.getElementById("ig-current").textContent=`Cancelación: ${error.message}`;}}
async function loadHistory(){const body=document.getElementById("ig-history");if(!body)return;try{const data=await api("/executions?capability=instagram.prospecting&limit=50"),data2=await api("/executions?capability=instagram.maduracion&limit=50"),items=[...(data.items||[]),...(data2.items||[])].sort((a,b)=>String(b.created_at).localeCompare(String(a.created_at))).slice(0,50);body.innerHTML=items.map((item)=>{const id=item.id||item.execution_id;return `<tr><td><code>${esc(id)}</code></td><td>${esc(item.requested_capability||item.capability)}</td><td>${esc(item.status)}</td><td>${esc(item.created_at)}</td><td><button class="button ghost ig-open" data-id="${esc(id)}">Abrir</button></td></tr>`;}).join("")||`<tr><td colspan="5" class="ig-muted">Sin ejecuciones Instagram.</td></tr>`;body.querySelectorAll(".ig-open").forEach((button)=>button.addEventListener("click",()=>{state.executionId=button.dataset.id;startPolling(state.executionId);}));}catch(error){body.innerHTML=`<tr><td colspan="5">${esc(error.message)}</td></tr>`;}}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",mount);else mount();
